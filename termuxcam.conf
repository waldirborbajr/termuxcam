# termuxcam configuration

# Capture interval
capture=3h

# Camera mode: 0=back only, 1=front only, 2=both
camera=1

# Motion detection
motion=true
motion_threshold=6

# Force upload at least this often even without motion (0 = disabled)
heartbeat=1h
